# Часть 48. Модуль брендов (IV): проверка формы добавления/редактирования бренда
В этой части мы улучшим модуль «Бренды», добавив серверную валидацию в форму «Добавить/Изменить бренд» с помощью проверки запроса формы. Это обеспечит понятный, удобный и безопасный ввод данных администратором.
## Шаг 1. Создайте BrandRequest для проверки формы
Начнем с создания пользовательского класса запроса формы, который обрабатывает всю логику проверки. Выполните следующую команду artisan:
```
php artisan make:request Admin/BrandRequest
```
## Шаг 2. Определите правила проверки и сообщения
Откройте вновь созданный файл **app/Http/Requests/Admin/BrandRequest.php** и обновите его следующим кодом:
```php
use App\Models\Brand;
use Illuminate\Contracts\Validation\Validator;
use Illuminate\Foundation\Http\FormRequest;
use Illuminate\Http\Exceptions\HttpResponseException;

class BrandRequest extends FormRequest
{
    /**
     * Determine if the user is authorized to make this request.
     */
    public function authorize(): bool
    {
        return true;
    }

    /**
     * Get the validation rules that apply to the request.
     *
     * @return array<string, \Illuminate\Contracts\Validation\ValidationRule|array<mixed>|string>
     */
    public function rules(): array
    {
        return [
            'name' => 'required',
            'url' => 'required|regex:^[\pL\s\-]+$/u',
        ];
    }

    /**
     * Пользовательские сообщения об ошибках проверки.
     */
    public function messages(): array
    {
        return [
            'name.required' => 'Обязательно укажите название бренда.',
            'url.required' => 'Обязательно укажите URL бренда.',
        ];
    }

    /**
     * Подготовка запроса перед проверкой.
     */
    protected function prepareForValidation(): void
    {
        if ($this->route('brand')) {
            $this->merge([
                'id' => $this->route('brand'),
            ]);
        }
    }

    /**
     * Пользовательская логика для проверки уникальности URL.
     */
    public function withValidator($validator): void
    {
        $validator->after(function ($validator) {
            $brandCount = Brand::where('url', $this->input('url'));
            if ($this->filled('id')) {
                $brandCount->where('id', '!=', $this->input('id'));
            }
            if ($brandCount->count() > 0) {
                $validator->errors()->add('url', 'Бренд уже существует!');
            }
        });
    }

    /**
     * Настройте ответ на ошибку проверки.
     */
    protected function failedValidation(Validator $validator)
    {
        throw new HttpResponseException(
            redirect()->back()->withErrors($validator)->withInput()
        );
    }
}
```
## Шаг 3. Обновление контроллера для использования BrandRequest
Обновите **BrandController**, чтобы использовать этот класс запроса формы в функциях хранения и обновления.

Сначала добавьте импорт вверху:
```php
use App\Http\Requests\Admin\BrandRequest;
```
Затем обновите методы:
```php
public function store(BrandRequest $request)
{
    $messages = $this->brandService->addEditBrand($request);
    return redirect()->route('brands.index')->with('success_message', $messages);
}

public function update(BrandRequest $request, string $id)
{
    $request->merge(['id' => $id]);
    $message = $this->brandService->addEditBrand($request);
    return redirect()->route('brands.index')->with('success_message', $message);
}
```
Основные преимущества этого обновления:
- Обеспечивает обязательное указание названия бренда и URL-адреса
- Проверяет формат URL с помощью регулярных выражений
- Проверяет наличие дубликатов URL-адресов брендов
- Показывает пользовательские сообщения об ошибках
- Повторно использует одну и ту же проверку для операций добавления и редактирования.
# Что дальше?
В [следующей части](49.md) мы покажем поле выбора брендов в форме «Добавить/редактировать продукт», чтобы администратор мог выбрать один бренд для каждого продукта.

[Оглавление](../README.md)
